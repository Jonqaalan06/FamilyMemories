@model FamilyMemories.Models.FamilyMemberDocumentViewModel
@{
}

<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.bundle.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.13.1/js/bootstrap-select.min.js"></script>

<h1>Upload Document</h1>
<hr/>
<div class="row">
    <div class="col-mt-3">
        <form asp-action="Upload" enctype="multipart/form-data">
            <div class="form-group">
                <label asp-for="DocumentDescription" class="control-label">Document Description</label>
                <input asp-for="DocumentDescription" class="form-control" />
            </div>
            <br />
            <div class="form-group">
                
                <input asp-for="Document" accept="*" class="form-control" />
            </div>
            <br />
            
            <div class="col-mt-3t">
                <select class="selectpicker" multiple data-live-search="true"
                        class="js-multiple-select form-control"
                        multiple="multiple"
                        asp-for="Options"
                        asp-items="@Model.Options">
                </select>
            </div>

            <label>Can't find the document owner in the list?
                <a href="/FamMembers/Add">
                     Add them please.
                </a>
            </label>
            <br />
            <br />
            <button type="submit" class="btn btn-primary" id="upload-btn">Upload Image</button>
            <input asp-for="FamMemberIds" type="hidden" />
        </form>
    </div>
</div>

<script src="~/js/site.js"></script>
